import random
import base64

key = 32465689


def shifr(key, o=False):
    listwayshifr = ['h', 'j', 'f', 'd', 'y']
    randomson = random.randint(1, 10)
    shifrway = random.choice(listwayshifr)
    if shifrway == 'h':


        key = str(key)


shifr(key)

